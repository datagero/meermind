// import * as RealmWeb from "realm-web";
import * as Realm from "realm-web";
// // Add your App ID
// export const app = new RealmWeb.App({ id: process.env.APP_ID || "" });
// const credentials = RealmWeb.Credentials.anonymous();
// try {
//   const user = await app.logIn(credentials);
// } catch(err) {
//   console.error("Failed to log in", err);
// }
export  const app = new Realm.App({ id: "application-0-qixyfgm" });
// const credentials = Realm.Credentials.anonymous();
// try {
//   const user = await app.logIn(credentials);
// } catch(err) {
//   console.error("Failed to log in", err);
// }

// const user = await app.logIn(credentials);
// const realm = await Realm.open({
//   schema: [Dog],
//   sync: { user, flexible: true },
// });

// await realm.subscriptions.update((subs) => {
//   const dogs = realm
//     .objects(Dog)
//     .filtered('name == "Clifford" && age > 5');
//   subs.add(dogs);
// });



